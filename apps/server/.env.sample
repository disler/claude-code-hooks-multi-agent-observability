# Server configuration
SERVER_HOST=0.0.0.0  # Use 0.0.0.0 to listen on all interfaces, localhost for local only
SERVER_PORT=4000     # Port for the HTTP/WebSocket server

# CORS settings (optional)
CORS_ORIGIN=*        # Set specific origin or * for all origins