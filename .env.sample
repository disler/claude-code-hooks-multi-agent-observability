# ============================================================================
# Observability System - Environment Configuration Template
# ============================================================================
#
# ⚠️  SECURITY WARNINGS ⚠️
#
# 1. NEVER commit the .env file to git (it's in .gitignore)
# 2. Copy this file to .env and fill in your actual values
# 3. Use SEPARATE keys for dev/staging/production environments
# 4. Rotate API keys every 30-90 days
# 5. Use LEAST-PRIVILEGE keys (minimum required permissions)
# 6. Store production secrets in a secrets manager (AWS Secrets Manager, Vault)
#
# ============================================================================

# ----------------------------------------------------------------------------
# Required Configuration
# ----------------------------------------------------------------------------

# Your name (for logging and identification)
# Used in: Event logging, session attribution
ENGINEER_NAME=YourName

# ----------------------------------------------------------------------------
# Optional: AI Summarization (Anthropic Claude)
# ----------------------------------------------------------------------------
#
# Purpose: Generate AI summaries of hook events for better readability
# Get key from: https://console.anthropic.com/keys
# Permissions needed: messages:write (for Claude API calls)
# Cost: ~$0.001-0.005 per event summary (very low usage)
#
# If left empty: Observability works fine, just no AI summaries
#
ANTHROPIC_API_KEY=

# ----------------------------------------------------------------------------
# Optional: Multi-Model Support (OpenAI)
# ----------------------------------------------------------------------------
#
# Purpose: Alternative AI model for event summarization
# Get key from: https://platform.openai.com/api-keys
# Permissions needed: chat.completions (for GPT API calls)
# Cost: Similar to Anthropic, ~$0.001-0.005 per summary
#
# If left empty: System will use Anthropic if available, or skip summaries
#
OPENAI_API_KEY=

# ----------------------------------------------------------------------------
# Optional: Voice Features (ElevenLabs)
# ----------------------------------------------------------------------------
#
# Purpose: Text-to-speech for event notifications (advanced feature)
# Get key from: https://elevenlabs.io/
# Permissions needed: text-to-speech API access
# Cost: Varies by usage, typically $0.001-0.01 per notification
#
# If left empty: No voice notifications (most users don't need this)
#
ELEVENLABS_API_KEY=

# ----------------------------------------------------------------------------
# Optional: Google Gemini Support
# ----------------------------------------------------------------------------
#
# Purpose: Alternative AI model (free tier available)
# Get key from: https://aistudio.google.com/app/apikey
# Permissions needed: generativelanguage.*
# Cost: FREE tier available, paid tier very cheap
#
# If left empty: No Gemini support (not required)
#
GEMINI_API_KEY=

# ----------------------------------------------------------------------------
# Server Configuration (Advanced)
# ----------------------------------------------------------------------------

# Server port (default: 4000)
# Change if port 4000 is already in use
# SERVER_PORT=4000

# Client dashboard port (default: 5173 - Vite dev server)
# CLIENT_PORT=5173

# Database path (default: apps/server/events.db)
# DB_PATH=apps/server/events.db

# Log level: DEBUG, INFO, WARN, ERROR (default: INFO)
# Use DEBUG only in development
LOG_LEVEL=INFO

# Enable audit logging (default: true)
# Always enable in production
ENABLE_AUDIT_LOG=true

# Max events to store in database (default: 10000)
# Older events are automatically pruned
# MAX_EVENTS=10000

# WebSocket ping interval in seconds (default: 30)
# WS_PING_INTERVAL=30

# ----------------------------------------------------------------------------
# Security Settings
# ----------------------------------------------------------------------------

# CORS allowed origins (default: http://localhost:5173)
# Add your domains if hosting remotely
# CORS_ORIGINS=http://localhost:5173,https://yourdomain.com

# Enable request rate limiting (default: true)
RATE_LIMIT_ENABLED=true

# Max requests per minute per IP (default: 100)
# RATE_LIMIT_MAX=100

# Session timeout in seconds (default: 3600 = 1 hour)
# SESSION_TIMEOUT=3600

# ============================================================================
# Setup Checklist
# ============================================================================
#
# Before starting the observability system:
#
# [ ] Copied this file to .env
# [ ] Set ENGINEER_NAME to your actual name
# [ ] (Optional) Added ANTHROPIC_API_KEY for AI summaries
# [ ] Verified .env is in .gitignore
# [ ] Reviewed security warnings above
# [ ] Tested with: ./scripts/validate-config.sh
#
# Start the system:
#   ./scripts/start-system.sh
#
# Verify it's running:
#   curl http://localhost:4000/health
#   # Expected: {"status":"operational"}
#
# ============================================================================

# ----------------------------------------------------------------------------
# Key Rotation Tracking (Optional but Recommended)
# ----------------------------------------------------------------------------
#
# Document when you created these keys so you know when to rotate:
#
# ANTHROPIC_API_KEY created: YYYY-MM-DD
# ANTHROPIC_API_KEY rotate by: YYYY-MM-DD (90 days later)
#
# OPENAI_API_KEY created: YYYY-MM-DD
# OPENAI_API_KEY rotate by: YYYY-MM-DD (90 days later)
#
# ============================================================================
